# Web 框架
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9

# 数据校验
pydantic==2.7.4

# 认证
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# 模板与报告
jinja2==3.1.6
weasyprint==62.1

# Agent 采集
psutil==5.9.8
wmi==1.5.1; platform_system == "Windows"
pywin32-ctypes; platform_system == "Windows"

# 工具
python-dateutil==2.9.0
python-dotenv>=1.0.0

# AI分析模块
httpx==0.27.0
cryptography==43.0.0
tiktoken>=0.7.0

# RAG向量检索（首次启动自动下载模型~80MB）
chromadb>=1.5.0
sentence-transformers>=3.0.0

# Markdown渲染（报告导出）
markdown>=3.4

# DOCX 导出
python-docx>=1.1.0

# 定时任务调度（Phase 2 — IOC 列表每日同步）
apscheduler>=3.10.0

# 手工日志导入（Manual Log Import）
python-evtx>=0.1.1
