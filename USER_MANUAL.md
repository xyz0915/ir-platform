# 应急响应平台 — 用户手册

> 版本：v1.0 | 更新日期：2026-07-14

---

## 目录

1. [日志检索模块](#1-日志检索模块)
2. [分析中心增强](#2-分析中心增强)
3. [案件管理](#3-案件管理)
4. [规则管理 & 策略配置](#4-规则管理--策略配置)
5. [白名单配置](#5-白名单配置)
6. [UI 设计规范概述](#6-ui-设计规范概述)
7. [常见问题](#7-常见问题)

---

## 1. 日志检索模块

### 1.1 功能概述

日志检索模块提供 Agent 上报日志的实时检索能力，支持全文搜索、字段过滤、时间范围筛选，以及一键生成安全事件。

### 1.2 案件-主机选择

页面顶部案件条带展示当前选中的案件和主机：

- **案件信息**：案件名称、编号、状态、日志数/事件数
- **主机信息**：主机名、IP 地址
- 点击「切换案件」按钮可切换案件

### 1.3 搜索面板

搜索面板默认展开，点击标题栏可折叠/展开。

#### 时间范围
- 预设：15 分钟 / 1 小时 / 近 24 小时 / 3 天 / 7 天 / 30 天 / 全部
- 支持自定义起止时间输入

#### 日志级别
- 多选 chip：CRITICAL / HIGH / MEDIUM / LOW / INFO / DEBUG
- 每个芯片右侧显示该级别的命中数量

#### 来源采集器
- 多选 chip：processes / network / registry / startup / services / files / ioc / wmi / users
- 选中状态以蓝色高亮表示

#### 关键字搜索
- 输入关键字后回车或点击搜索按钮
- 支持搜索语法：

| 语法 | 示例 | 说明 |
|------|------|------|
| `pid:{数字}` | `pid:4352` | 按进程 PID 搜索 |
| `severity:{级别}` | `severity:high` | 按严重度搜索 |
| `ip:{地址}` | `ip:8.8.8.8` | 按 IP 地址搜索 |
| `name:"字符串"` | `name:"powershell"` | 按名称精确搜索 |
| `collector:{名称}` | `collector:network` | 按采集器搜索 |

### 1.4 快速筛选条

提供快捷筛选入口，一键筛选常见场景：

- 高危未处理
- IOC 命中
- 今日新增
- 可疑进程
- 外部连接
- Base64 编码

### 1.5 结果列表

日志结果以表格形式展示，包含以下列：

| 列 | 说明 |
|------|------|
| **级别** | 严重度标识（色点 + 标签）：CRITICAL(红)、HIGH(红)、MEDIUM(琥珀)、LOW(蓝)、INFO(灰) |
| **时间** | 日志采集时间（ISO 格式） |
| **来源** | 采集器名称 + 图标 |
| **主机** | 主机名缩写 |
| **摘要** | 日志内容预览，关键字高亮显示 |

**交互操作**：
- 点击行：选中该日志并在右侧详情面板展示完整信息
- 鼠标悬停：显示查看详情和复制按钮
- 复选框：支持多选
- 表头排序：点击「级别」「时间」表头可排序

### 1.6 详情面板

选中某条日志后，右侧详情面板展示：

| 区域 | 内容 |
|------|------|
| **基本信息** | 级别、时间、采集器、主机、案件、日志 ID |
| **AI 分析摘要** | 自动分析结果，含置信度和关联攻击框架提示 |
| **原始 JSON** | 语法高亮的完整 JSON 数据，支持复制和格式化 |
| **字段过滤** | 点击 Tag 快速添加过滤条件 |
| **关联操作** | 跳转到事件分析、加入攻击链、标记为误报、添加备注 |

### 1.7 保存搜索条件

点击「保存到」按钮，可保存当前搜索条件到本地浏览器，下次重复使用。

---

## 2. 分析中心增强

### 2.1 功能概述

分析中心展示安全事件列表，支持多维度筛选、事件详情查看、跳转原始日志。

### 2.2 事件列表

| 列 | 说明 |
|------|------|
| **案件** | 事件所属案件名称（悬停显示案件 ID） |
| **主机** | 事件来源主机名 + IP 地址 |
| **事件类型** | 安全事件类型（如 process_start、network_outbound） |
| **严重度** | 事件严重等级，左侧色条标识 |
| **时间** | 事件发生时间 |

### 2.3 事件详情

点击事件行显示详情面板，包含：

- **基本信息**：事件类型、时间、主机、案件
- **匹配规则**：触发该事件的具体规则名称、描述、严重度
- **原始命令/数据**：具体触发数据
- **关联数据**：「查看原始日志」按钮 — 跳转到日志检索页并自动带入该事件的条件（案件、主机、关键字）

---

## 3. 案件管理

### 3.1 功能概述

案件管理页面管理所有应急响应案件，支持新建、搜索、筛选、排序、删除。

### 3.2 新建案件

点击「新建案件」按钮弹出创建表单：

| 字段 | 说明 |
|------|------|
| **案件名称**（必填） | 输入案件名称 |
| **案件编号** | 可选。不填则自动生成编号 |
| **优先级** | 下拉选择：严重 / 高 / 中 / 低。不选默认为"中" |
| **描述** | 可选，输入案件描述 |

#### 案件编号自动生成

- **格式**：`CASE-{YYYYMMDD}-{NNNN}`
- **示例**：`CASE-20260714-0001`、`CASE-20260714-0002`
- **规则**：当日序号从 0001 起自增，次日从 0001 重新开始
- **手动填写**：用户填写的编号必须全局唯一，重复时系统拒绝保存

### 3.3 案件列表

| 列 | 说明 |
|------|------|
| **案件** | 案件名称 + 副标题（主机数、日志数） |
| **案件编号** | 自动生成或手动填写的编号 |
| **描述** | 案件描述摘要 |
| **优先级** | 彩色圆点 + 中文标签：严重(🔴)、高(🟠)、中(🔵)、低(🟢) |
| **主机** | 案件关联的主机数 |
| **状态** | 案件状态（进行中、已暂停、已归档等）|
| **创建时间** | 案件创建时间 |
| **操作** | 查看详情、编辑、导出、删除按钮 |

### 3.4 搜索与筛选

- **搜索框**：按案件名称或编号搜索
- **状态筛选 chip**：全部状态 / 进行中 / 已暂停 / 已归档
- **时间范围**：最近 7 天 / 30 天 / 90 天 / 全部时间

---

## 4. 规则管理 & 策略配置

### 4.1 规则管理

规则管理页面展示所有检测规则，支持搜索、筛选、编辑、启用/禁用。

#### 规则列表

| 列 | 说明 |
|------|------|
| **规则名** | 图标 + 英文名 + 中文名 + ATT&CK 标签 |
| **类别** | 规则所属类别（进程行为、网络连接等） |
| **严重度** | 严重度色点 + 文字 |
| **ATT&CK** | MITRE ATT&CK 编号 |
| **描述** | 规则描述 |
| **来源** | 内置灰色 / 用户蓝色 badge |
| **启用** | CSS 滑动开关，绿色=启用，灰色=禁用 |
| **操作** | 查看详情、编辑、删除 |

#### 批量操作

勾选多条规则后，出现批量操作条：
- 批量启用
- 批量禁用

#### 规则详情弹窗

点击查看规则详情，展示规则名称、类别、条件 JSON、匹配模式等。

### 4.2 策略配置

策略配置页面采用双列布局：左侧策略列表 + 右侧策略详情。

#### 策略列表

展示所有检测策略，每项显示：
- 策略名称 + 已激活徽章（绿色）
- 策略描述
- 规则数、RAG 开关状态
- 蓝色圆点表示当前选中态

#### 策略详情

选中策略后右侧展示详情：

| 区域 | 说明 |
|------|------|
| **策略名称/描述** | 可编辑 |
| **策略开关** | RAG 语义分析、攻击链检测开关 |
| **规则选择** | 规则选择器，支持按类别、严重度、关键字筛选规则 |
| **Tabs** | 规则 / 范围 / 触发条件 / 变更历史 |

#### 规则选择

- 规则列表展示规则名、类别标签、严重度色点 + 文字
- 开关勾选规则，已选数量显示在顶部
- 支持分页浏览和跨页选中
- 点击「保存规则选择」保存修改

---

## 5. 白名单配置

### 5.1 功能概述

白名单配置页面管理系统内置和用户自定义的白名单规则，命中后跳过检测引擎告警。

### 5.2 白名单列表

| 列 | 说明 |
|------|------|
| **类别** | 图标 + 类别 badge（路径/进程名/签名）|
| **匹配值** | 等宽字体的匹配值 + 提示说明 |
| **来源** | 内置(灰) / 用户(蓝) badge |
| **描述** | 白名单规则描述 |
| **命中统计** | 等宽数字显示命中次数 |
| **启用** | CSS 滑动开关 |

- 内置规则：灰色显示，编辑按钮禁用
- 用户规则：可编辑、可删除

---

## 6. UI 设计规范概述

### 6.1 设计语言

本平台采用中性色系设计语言，参考 Linear / Vercel / Stripe 风格：

- **色彩**：黑/白/灰中性色系，主题色（蓝色）占比不超过 5%
- **平面设计**：无渐变、无 emoji 图标、无阴影
- **圆角分层**：按钮 6px / 卡片 10px / 容器 12px
- **边框**：所有组件使用 0.5px 实线边框代替阴影
- **字重**：仅使用 400（常规）和 500（中等）
- **间距**：全部使用 8 的倍数

### 6.2 组件风格

| 组件 | 风格特征 |
|------|---------|
| **按钮** | 圆角 6px，字重 500，default/primary/danger/ghost 四种 |
| **卡片** | 圆角 10px，0.5px 边框，无阴影 |
| **表格** | 圆角 10px 容器，斑马色行，hover 高亮 |
| **输入框** | 圆角 6px，focus 蓝色边框 |
| **搜索条** | 圆角 6px，focus 蓝色边框 |
| **Badge** | 圆角 4px，语义色 |
| **弹窗** | 圆角 12px，半透明遮罩 |

---

## 7. 常见问题

### Q1: 刷新后数据不见了？

**可能原因**：后端服务未正常启动。

**解决**：
```bash
# 重启后端
cd backend
taskkill /F /IM python.exe
./venv/Scripts/python.exe -m uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
```

### Q2: 页面样式错乱，老样式还在？

**可能原因**：浏览器缓存了旧的 JS/CSS 文件。

**解决**：
```bash
# 强制刷新（清除缓存）
Ctrl+Shift+R
```
如果仍然无效，尝试重启 Vite 前端：
```bash
# 终端中 kill 并重启前端
taskkill /F /IM node.exe
cd frontend && npm run dev
```

### Q3: 日志检索没有数据显示？

**排查步骤**：
1. 确认后端已启动（浏览器访问 `http://localhost:8000/docs` 看是否正常）
2. 确认已导入 JSON 数据（进入某个案件，看顶部统计是否有日志数）
3. 确认搜索时间范围覆盖了数据导入时间
4. 按 F12 → Console 查看是否有红色报错

### Q4: 新建案件时编号重复？

系统会提示冲突，请更换为其他唯一编号，或留空由系统自动生成。

### Q5: 导入 JSON 后数据不增加？

导入流程新增了自动写入日志检索和分析中心的能力，但需要：
1. 确保数据格式符合 Agent JSON 规范
2. 导入后刷新页面查看统计数字是否更新
3. 如仍无数据，请确认目标主机关联到了正确的案件

---

*应急响应平台 — IRIS 用户手册*
*最后更新：2026-07-14*
